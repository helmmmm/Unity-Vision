using UnityEngine;

public class VectorMovement : MonoBehaviour
{
    public float movementSpeed = 5.0f;
    private Rigidbody rb;

    void Start()
    {
        // Get the Rigidbody component attached to this GameObject
    }

    void Update()
    {
        // Create a Vector3 variable to store the movement direction

        // Change the movement direction based on the arrow keys

        // Create a Vector3 variable to store the movement amount and direction
        
        // Move the Rigidbody in the direction of the movement vector from its current position
    }
}
